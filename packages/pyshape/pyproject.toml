[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pyshape"
version = "0.1.0"
description = "A Python library for geometric shapes and area calculations"
dependencies = [
    "pytest",
]

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = ["shapes"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]